{
  "name": "abdellah-moutal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting",
    "firebase": "firebase"
  },
  "devDependencies": {
    "firebase-tools": "^15.30.1",
    "vite": "^8.3.0"
  }
}
